import { OrderRecipesPipe } from './order-recipes.pipe';

describe('OrderRecipesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderRecipesPipe();
    expect(pipe).toBeTruthy();
  });
});
